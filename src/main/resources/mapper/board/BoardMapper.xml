<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.myapp.board.dao.IBoardRepository">
<!-- 
	
	<insert id="insertArticle" parameterType="com.project.myapp.board.model.Board">
	<![CDATA[
		INSERT INTO 
		board 
			(board_id, member_id, title, content, views, create_at, update_at)  
		VALUES 
			(#{boardId}, #{memberId}, #{title}, #{content}, #{views}, SYSDATE, SYSDATE)
	]]>
	</insert>
	

	<insert id="replyArticle" parameterType="com.project.myapp.board.model.Board">
	<![CDATA[
		INSERT INTO 
		board 
			(reply_id, member_id, board_id, content, create_at, update_at)  
		VALUES 
			(#{replyId}, #{memberId}, #{boardId}, #{content}, SYSDATE, SYSDATE)
	]]>
	</insert>
		
	
	<update id="updateArticle" parameterType="com.project.myapp.board.model.Board">
	<![CDATA[
		UPDATE 
			board 
		SET 
			title=#{title}, 
			content=#{content}, write_date=SYSDATE 
		WHERE 
			board_id=#{boardId}
	]]>
	</update> -->
</mapper>